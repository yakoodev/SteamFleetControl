@model LoginRequest
@{
    ViewData["Title"] = "Вход";
}

<div class="row justify-content-center">
    <div class="col-xl-4 col-lg-5 col-md-7">
        <section class="sf-card">
            <div class="sf-card-head">
                <h1 class="sf-title">Вход в SteamFleet</h1>
                <div class="sf-subtitle">Управление Steam-аккаунтами, jobs и аудитом</div>
            </div>
            <div class="sf-card-body">
                <form asp-action="LoginPost" method="post">
                    <input type="hidden" name="returnUrl" value="@ViewData["ReturnUrl"]" />
                    <div class="mb-3">
                        <label class="form-label" asp-for="Email"></label>
                        <input class="form-control" asp-for="Email" />
                        <span class="text-danger" asp-validation-for="Email"></span>
                    </div>
                    <div class="mb-3">
                        <label class="form-label" asp-for="Password"></label>
                        <input type="password" class="form-control" asp-for="Password" />
                        <span class="text-danger" asp-validation-for="Password"></span>
                    </div>
                    <div class="form-check mb-3">
                        <input class="form-check-input" asp-for="RememberMe" />
                        <label class="form-check-label" asp-for="RememberMe">Запомнить меня</label>
                    </div>
                    <button class="btn sf-btn sf-btn-primary w-100" type="submit">Войти</button>
                </form>
            </div>
        </section>
    </div>
</div>
